# LegacyLog

**Claude Code 원격 게임 개발 워크플로우 검증 실험**

> 최종 수정: 2026-04-11
> 변경 이력은 문서 하단 참조.

---

## 이 저장소는 무엇인가

이 저장소는 두 가지 정체성을 동시에 가진다:

1. **Git 저장소** — Godot + C# 게임 프로젝트 `legacy-log-project/`와 실험 문서 `docs/`를 관리한다.
2. **Obsidian Vault** — 실험 관찰 로그를 `docs/experiment-log/`에 누적한다. `legacy-log-project/`는 Obsidian의 제외 필터로 시야에서 빠져 있다.

## 실험의 핵심 질문

> **Godot 엔진은 Claude Code 원격 사이드 프로젝트용 게임엔진으로 적합한가?**

부차적 질문:
- A+C 워크플로우(집 PC + 모바일 자투리 시간)를 CI 자동 빌드·알림으로 보강하면 실용적으로 작동하는가?
- Claude Code에게 어떤 종류의 작업을 맡길 수 있고, 어떤 종류는 사람이 직접 해야 하는가?

## 게임은 실험의 도구다

이 저장소가 만드는 게임(LegacyLog, 가문 연대기 로그라이크)은 실험의 **산출물이 아니라 매개체**다. 게임 완성 자체가 목적이 아니라, 게임을 만드는 과정에서 드러나는 Claude Code × Godot 워크플로우의 강점과 한계를 측정하는 것이 목적이다.

게임의 구체적 설계·규칙·데이터 구조는 이 문서가 다루지 않는다. 구현 관점의 맥락은 `legacy-log-project/CLAUDE.md`를 참조한다.

## 작업 환경 전제

- **A 모드**: 집 PC에서 Claude Code 중심, Godot 에디터는 최소한으로
- **C 모드**: 이동 중 모바일에서 자투리 작업 (JSON 편집, 지시 전달 등)

설계 원칙: *"집에서는 에디터 가능, 이동 중엔 텍스트만으로 모든 게 가능해야 한다."*

---

## 문서 아키텍처

### 저장소 구조

```
legacy-log/                        ← Git 루트 = Obsidian Vault 루트
├── CLAUDE.md                      ← (이 문서) 메타 에이전트용
├── .gitignore                     ← 프로젝트 전체 수준 제외 규칙
├── .obsidian/                     ← Obsidian 설정 (workspace.json 등 일부 제외)
├── docs/
│   ├── decisions/                 ← 결정 기록. 한 결정 = 한 파일
│   ├── experiment-log/            ← 세션별 실험 관찰 로그
│   └── current-focus.md           ← 지금 작업 중인 초점 (한 페이지)
└── legacy-log-project/            ← Godot C# 게임 프로젝트
    └── CLAUDE.md                  ← 구현 에이전트용 (이 문서의 시야 밖)
```

### 두 CLAUDE.md의 역할 분담

| 구분 | 루트 `CLAUDE.md` (이 문서) | 하위 `legacy-log-project/CLAUDE.md` |
|---|---|---|
| 관점 | 실험 관찰자 | 구현자 |
| 다루는 것 | 실험 목적, 문서 아키텍처, 관찰 원칙 | 게임 컨셉, 데이터 규칙, 코딩 컨벤션 |
| 대상 에이전트 | 메타 에이전트 | 구현 에이전트 |
| 작업 디렉토리 | 저장소 루트 | `legacy-log-project/` 내부 |

**두 문서는 내용을 중복시키지 않는다.** 구현 세부사항(예: "JSON이 Single Source of Truth")은 하위 CLAUDE.md에만 존재한다. 이 문서는 그 규칙의 존재를 모르고, 알 필요도 없다.

### 메타 에이전트의 작업 영역

**읽고 쓸 수 있는 영역:**
- `CLAUDE.md` (이 문서)
- `docs/` 전체
- `README.md`
- `.gitignore`, `.obsidian/` 설정 파일

**읽을 수는 있으나 수정은 금지되는 영역:**
- `legacy-log-project/` 내부 전체 — 구현 에이전트의 관할

**예외:** 실험 관찰을 위해 `legacy-log-project/` 내부 파일의 *존재 여부*나 *Git 히스토리*를 조회하는 것은 허용된다. 단, 파일 내용 편집은 금지된다.

### 문서의 종류와 작성 시점

- **`docs/decisions/NNN-주제.md`** — 설계·전략 결정이 내려진 순간에 작성. 번호는 순차. 결정의 근거, 고려한 대안, 최종 선택을 남긴다.
- **`docs/experiment-log/YYYY-MM-DD-주제.md`** — 작업 세션 후 관찰을 기록. Frontmatter + 자유 본문 하이브리드 형식.
- **`docs/current-focus.md`** — 지금 진행 중인 작업 초점 한 페이지. 세션 시작 시 에이전트가 먼저 읽는 파일. 초점이 바뀔 때마다 업데이트.

---

## 실험 관찰 원칙

이 실험은 "Claude Code에게 무엇을 어떻게 위임할 수 있는가"를 측정한다. 그러려면 위임과 관찰 자체에 규율이 필요하다. 아래 원칙은 실전 세션에서 도출되었으며, 새 원칙이 발견되면 이 문서에 추가한다.

### 원칙 1: 되돌리기 비용이 꺾이는 지점 전에 멈춘다

어떤 작업은 되돌리기가 쉽고, 어떤 작업은 한 번 실행하면 복구가 기하급수적으로 비싸진다. 이 경계 직전에 **반드시 사람의 검증을 받는 멈춤 지점**을 둔다.

**예시 (Git):**
- `git init` → 되돌리기 쉬움 (`.git` 삭제)
- `git add` → 되돌리기 쉬움 (`git reset`)
- `git commit` → 가능하지만 번거로움 ← **여기 직전에 멈춤**
- `git push` → 매우 번거로움 (force push, 협업자 영향)

**적용 대상:**
- 파일 대량 삭제·이동·이름 변경
- 데이터베이스 스키마 변경
- 원격 저장소·서비스에 대한 쓰기
- 되돌릴 수 없는 API 호출

### 원칙 2: 가정을 명시하고, 가정 위반 시 멈춘다

위임 지시문에는 "이 작업은 X 상태를 전제로 한다"는 **사전 상태 가정**을 명시한다. 에이전트가 실제로 X가 아니라는 걸 발견하면 추측으로 진행하지 말고 멈춘다.

**효과:** 가정이 틀려서 발생하는 실패가 "조용한 오동작"이 아니라 "큰소리 나는 멈춤"으로 드러난다. 실패가 빠르고 명확할수록 복구가 싸다.

### 원칙 3: 위임 지시문은 다섯 가지 요소를 갖춘다

Claude Code에게 작업을 위임할 때, 지시문은 다음 다섯 가지를 포함해야 한다:

1. **목적** — 왜 이 일을 하는지. 에이전트가 예외 상황에서 현명하게 판단할 근거.
2. **범위** — 어디까지 하고, 어디부터는 안 할지.
3. **구체적 행동** — 실행할 단계. 명령어까지 지정할지, 방식만 지정할지는 작업의 성격에 따른다.
4. **제약** — "하지 말 것" 목록. 범위의 부정적 표현. 민감한 작업일수록 명시적일 것.
5. **보고 형식** — 결과를 어떻게 돌려받을지. 표, 목록, 자유 서술 등.

민감 영역(사용자 환경·인증·보안 관련)을 다루는 지시문은 여기에 **민감 정보 마스킹 규칙**을 추가한다.

### 원칙 4: 관찰은 세션별로, 패턴 추출은 사후에

실험 로그는 `docs/experiment-log/`에 **세션별 파일**로 쌓는다. 세션 중에는 주제별 분류나 패턴 추출을 강요하지 않는다 — 쓰기 부담이 로그 자체를 억제한다.

패턴 추출은 로그가 충분히 쌓인 뒤 별도의 분석 세션에서 수행한다. 이 분석이 메타 에이전트의 주된 용도 중 하나다.

### 원칙 5: Claude Code의 "판단 유보"를 긍정 신호로 취급한다

에이전트가 애매한 지점에서 자기 판단으로 진행하지 않고 "이건 어떻게 할까요?"라고 멈추는 것은 **결함이 아니라 바람직한 동작**이다. 로그에 긍정 관찰로 기록한다.

반대로 에이전트가 지시 범위 밖의 일을 자의적으로 수행하면(예: 요청하지 않은 README 추가) 이는 주의 관찰 대상이다.

---

## 메타 에이전트의 역할 범위

### 기본 정체

메타 에이전트는 **저장소 루트(`legacy-log/`)에서 열린 Claude Code 세션**을 의미한다. 구현 에이전트(`legacy-log-project/`에서 열린 세션)와는 작업 디렉토리로 구분된다.

메타 에이전트는 실험 자체의 **관찰자·기록자·구조 관리자**다. 게임 구현은 하지 않는다.

### 주된 업무

메타 에이전트는 **이벤트 기반**으로 열린다. 다음 네 가지 업무가 주된 호출 사유다.

**1. 실험 로그 패턴 추출**
- 트리거: 사용자 판단으로 요청 시.
- 작업: `docs/experiment-log/`의 로그들을 읽고 반복되는 관찰 패턴, 미해결 병목, 유효한 위임 패턴을 요약.
- 산출물: `docs/experiment-log/summary-YYYY-MM-DD.md` 또는 사용자에게 인라인 보고.

**2. 결정 문서화**
- 트리거: 외부(다른 Claude 대화, 오프라인 사고 등)에서 설계 결정이 내려진 후 사용자가 요청할 때.
- 작업: 결정의 배경, 고려한 대안, 최종 선택, 근거를 `docs/decisions/NNN-주제.md`로 기록.
- 원칙: 결정 내용을 **창작하지 않는다**. 사용자가 제공한 맥락을 정리하는 역할이다.

**3. 작업 초점 업데이트**
- 트리거: 작업 단계가 바뀔 때 사용자가 요청.
- 작업: `docs/current-focus.md`를 현재 상태에 맞게 갱신. 이전 내용은 필요시 `docs/experiment-log/`의 해당 날짜 로그로 이전.

**4. 문서 정합성 검사**
- 트리거: 사용자 요청, 또는 다른 업무 중 모순 발견 시.
- 작업: 루트 `CLAUDE.md`, 하위 `CLAUDE.md`, `docs/decisions/` 사이에 서로 모순되는 규칙·원칙이 있는지 확인하고 보고.
- 원칙: 모순을 **자의적으로 해결하지 않는다**. 발견만 보고하고 사용자 판단을 기다린다.

### 하지 않는 일

메타 에이전트는 다음을 하지 않는다. 이 일들은 구현 에이전트 또는 사용자의 몫이다.

- `legacy-log-project/` 내부 파일 수정
- 게임 컨셉·규칙·데이터 구조에 대한 결정
- 새로운 실험 원칙의 **창작** (관찰에서 발견되는 경우는 제외하되, 최종 확정은 사용자 승인 후)
- 아직 정해지지 않은 방향성에 대한 추측성 문서화

### 세션 시작 시 행동

메타 에이전트는 세션 시작 시 다음 순서로 맥락을 복구한다.

1. 이 문서(`CLAUDE.md`) 전체 읽기
2. `docs/current-focus.md` 읽기 (지금 뭐 하는 중인지)
3. 사용자의 첫 지시 대기

맥락 복구 없이 곧바로 행동하지 않는다.

---

## 변경 이력

- **2026-04-11**: 초안 작성. 네 개 섹션(프로젝트 개요, 문서 아키텍처, 실험 관찰 원칙, 메타 에이전트 역할 범위) 확립.
